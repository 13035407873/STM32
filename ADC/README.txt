1.测试STM32 ADC模块各个功能
2.熟悉ADC模块原理
3.寄存器操作，最好看英文手册
4.整理一个STM32 ADC模块的文档

TEST01----独立工作、非扫描、单次转换、软件触发模式（规则通道）
TEST02----独立工作、非扫描、连续转换、软件触发模式（规则通道）
TEST03----独立工作、扫描、连续转换、DMA、软件触发模式（规则通道）
TEST04----独立工作、非扫描、单次转换、定时器触发模式（规则通道）
TEST05----规则通道模拟看门狗
TEST06----独立工作、非扫描、单次转换、外部硬件触发模式（规则通道）
TEST07----独立工作、扫描、连续转换、软件触发模式（规则通道+注入通道）
TEST08----同步注入模式
TEST09----同步规则模式
TEST10----快速交叉模式
TEST11----慢速交叉模式（循环）
TEST12----交替模式
TEST13----混合的同步规则/同步注入模式
TEST14----混合的同步规则 + 交替触发模式
TEST15----混合同步注入+快速交叉模式
TEST16----混合同步注入+快速交叉模式
TEST17----规则通道间隔模式